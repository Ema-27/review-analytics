# In sviluppo locale non serve impostarla (Vite fa proxy verso :8000, vedi vite.config.js).
# In build di produzione (Docker) il Front-End chiama semplicemente /api,
# instradato da nginx verso il container backend.
VITE_API_BASE_URL=/api/v1
