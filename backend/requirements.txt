fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg[binary]==3.2.2
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
python-dotenv==1.0.1

# NLP: classificazione sentiment locale (Hugging Face)
transformers==4.44.2
sentencepiece==0.2.0
# torch: nell'immagine Docker viene installata la build CPU-only (~3 GB in meno)
# dal Dockerfile. Per lo sviluppo locale senza Docker, installarla a parte:
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu
torch==2.4.1

# Test
pytest==8.3.3
pytest-cov==5.0.0
